/** Right-hand ToolActivity / workspace panel tab ids (keep in sync with ToolActivity.svelte). */
export type ToolActivityPanelTab =
  | "workspace"
  | "tools"
  | "context"
  | "files"
  | "info"
  | "tasks"
  | "preview"
  | "scheduled-tasks";
