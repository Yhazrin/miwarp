# MiWarp v1.1.0 RC 冻结条件检查表

> **基线**：分支 `feat/v1.1.0-final`，HEAD `e35bfdc2`。
> **检查日期**：2026-06-25。
> **配套文档**：`docs/PLAN_V1.1.0.md` §十 + §十一、`docs/RELEASE_NOTES_V1.1.0.md`、`docs/core-experience-v1.md`。
> **状态含义**：
> - `DONE` 完全通过
> - `PARTIAL` 主要功能完成，剩余验证 / 门禁 / 抽样未齐，附降级决策
> - `NOT-STARTED` 未启动
> - `DEFERRED` 明确不进入 v1.1.0 主线
> - **风险等级**：🔴 阻断 / 🟡 跟踪 / 🟢 已知且无影响

---

## 第 1 条 · 所有 P0 锚点达到验收标准或有书面降级决策

| ID        | 标题                                  | 状态        | 证据 | 风险 |
| --------- | ------------------------------------- | ----------- | ---- | ---- |
| `110-A0`  | Core Experience v1 Graduation         | `PARTIAL`  | `docs/core-experience-v1.md` §8（1,627 frontend / 462 Rust / 84 iOS / 48 Android tests pass），但 §3.7 / §3.8 / §3.11 / §3.12 缺真实 p50/p95 数据 | 🔴 DEFERRED 至 v1.1.1；7 天 soak 未完成 |
| `110-A1`  | Conversation Workbench                | `PARTIAL`  | `src/lib/components/chat/ChatTimelineEntries.svelte` 收敛为单一时间线；progressive timeline 在 `src/lib/chat/` | 🟡 10,000 Event FPS 预算缺采样；不影响 RC |
| `110-A2`  | Interactive Visual Intelligence       | `PARTIAL`  | `src/lib/components/ClaudeCanvas.svelte`、Mermaid 交互、`src/lib/skills/visualization` | 🟡 Vega/KPI/Mind Map Host 未齐；不影响 RC |
| `110-A3`  | Trustworthy Change Review             | `PARTIAL`  | `DiffModal` / `DiffPreview` / `WorkspaceInspector` | 🟡 行级评论 / Checkpoint Diff 未齐；不影响 RC |
| `110-A4`  | Runtime Control Tower                 | `PARTIAL`  | `src/lib/components/runtime/RuntimeControlCenter.svelte` 等 4 个组件 + `RuntimeCapabilities` typed mirror | 🟡 Tier 1 matrix 最近验证时间未自动化；不影响 RC |
| `110-A8`  | Worktree Task Lab                     | `PARTIAL`  | `src-tauri/src/task_core.rs` + `src/lib/components/tasks/*` + `src/routes/tasks/+page.svelte`（`9272dc08`） | 🟡 验证闭环 UI 未齐；不影响 RC |
| `110-A10` | Intervention Layer                    | `PARTIAL`  | 权限/AskUser/Stop 已有 | 🟡 Plan Gate / Checkpoint Pause 未齐；不影响 RC |
| `110-A13` | Artifact Center                       | `PARTIAL`  | `FileCard` / `FilePreviewPane` / `DiffModal` / `LinkCard` / `HtmlReportPreview` | 🟡 聚合主页面未齐；不影响 RC |
| `110-A21` | Native Window Surface Contract        | `DONE`     | `2b5ec6cb` `.app-main-shell` + `sidebar-main-corner-bridge` 与原生 glass wash 对齐 | 🟡 截图视觉回归门禁未自动化入库，跟踪项 |
| `110-S1`  | Durable Run Journal                   | `DONE`     | `src-tauri/src/run_core.rs` + `src-tauri/src/storage/run_journal.rs` / `run_journal/{reconcile,tests}.rs` + `cfc1111b` | 🟢 |
| `110-S2`  | Local Agent Trace                     | `PARTIAL`  | Trace 存储 / 按需导出在 | 🟡 Doctor UI / 一键脱敏导出未齐；不影响 RC |
| `110-G1`  | Diagnostics & Performance Gate        | `PARTIAL`  | `docs/perf/v109-performance-contract.md` + `scripts/perf-compare.mjs` + `src/lib/perf/*` | 🟡 发布门禁 p95 未齐；不影响 RC |
| `110-R1`  | Architecture Evolution                | `PARTIAL`  | `.codex/skills/architecture-lifecycle/SKILL.md` + `scripts/architecture/*` | 🟡 持续节奏进行中；不影响 RC |

**汇总**：13 个 P0 锚点 → `DONE 2` / `PARTIAL 11`。所有 PARTIAL 项有书面降级决策（DEFERRED 至 v1.1.1 / v1.2.0），满足冻结条件。

---

## 第 2 条 · Core Experience v1 毕业

**状态**：`PARTIAL` → 🟡 跟踪

- ✅ 12 领域毕业标准在 `docs/core-experience-v1.md` §3 已定义；
- ✅ 截至 2026-06-21 测试基线：1,627 frontend / 462 Rust / 84 iOS / 48 Android；
- ❌ 7 天真实项目 soak 未完成；
- ❌ Tier 1 provider 全量 E2E 矩阵未毕业；
- ❌ 关键性能预算 p50/p95 缺真实采样。

**降级决策**：以 PARTIAL 状态进入 RC；7 天 soak + Tier 1 全量矩阵列为 v1.1.1 RC 前置门禁。

---

## 第 3 条 · Task → Run → Diff → Verification → Artifact → Review 闭环可用

**状态**：`PARTIAL` → 🟡 跟踪

- ✅ **Task → Run**：`task_core.rs` + `run_core.rs` 已闭环；WAL 支持中断恢复；启动顺序 `reconcile_orphaned_runs → run_journal::reconcile_after_restart → tasks::reconcile_after_restart → attention_queue::reconcile`；
- ✅ **Run → Diff**：Run Journal 绑定 action_id；`DiffModal` 显示 Agent 修改；
- 🟡 **Verification**：内建单元测试覆盖，但 Task 级 Verification Report 进入 Artifact Center 闭环未齐；
- 🟡 **Artifact**：单卡层（`FileCard` / `LinkCard` / `HtmlReportPreview`）在；Artifact Center 聚合页未齐；
- 🟡 **Review**：Attention Queue `Final Review` 类型已纳入 schema；UI 入口未齐。

**降级决策**：以 PARTIAL 状态进入 RC；闭环 UI 收口随 v1.1.1 推进。

---

## 第 4 条 · Runtime capability matrix 不再依赖手工猜测

**状态**：`PARTIAL` → 🟡 跟踪

- ✅ `RuntimeCapabilities` typed mirror 在前端 `src/lib/types/runtime-capabilities.ts` 与 Rust `src-tauri/src/agent/control_plane/adapters/*` 双端；
- ✅ `runtime_hub_*` 命令集合在 `src-tauri/src/commands/runtime_hub.rs`；
- 🟡 「最近验证时间」字段已存在但由手工填写，缺自动化采集脚本；
- 🟡 Tier 1 / Experimental 标签在 `RuntimeControlCenter` 显示但与 `runtime_hub` 同步逻辑待补。

**降级决策**：以 PARTIAL 状态进入 RC；自动化采集脚本列为 v1.1.1 跟踪项。

---

## 第 5 条 · App / CLI / Browser / MCP 故障不会造成静默数据损失

**状态**：`PARTIAL` → 🟡 跟踪

- ✅ **App 崩溃**：Run Journal / Task WAL 持久化；启动时 reconcile_after_restart 收敛；
- ✅ **CLI 崩溃**：recovery state machine + typed runtime error；不自动盲重试非幂等动作；区分 `safe retry / manual confirmation / impossible resume`；
- ✅ **断网**：WebSocket 单飞恢复 + 序列号对齐；不重复用户消息；
- ✅ **休眠 / 唤醒**：Attention Queue 与 run journal `pending_approvals` 收敛；
- 🟡 **Browser crash**：Dev Server / Browser 隔离在 `BrowserPanel`，但未与 Run / Task 联动；
- 🟡 **MCP 故障**：失败次数与熔断在 `McpStatusPanel`，但 server 启动有界超时未完整接入。

**降级决策**：以 PARTIAL 状态进入 RC；Browser 与 MCP 故障联动在 v1.1.1 推进。

---

## 第 6 条 · 关键性能指标有真实 p95 数据

**状态**：`PARTIAL` → 🟡 跟踪

- ✅ Perf harness + benchmark + compare 脚本已建立（`scripts/perf-compare.mjs` + `docs/perf/v109-performance-contract.md`）；
- 🟡 10,000 Timeline Event ≥ 55 FPS / 10,000 行 Diff p95 ≤ 500ms / 冷启动 ≤ 150ms 等目标未在真实仓库上采样。

**降级决策**：以 PARTIAL 状态进入 RC；p95 真实采样列为 v1.1.1 RC 前置门禁。

---

## 第 7 条 · 7 天真实项目 soak 完成

**状态**：`NOT-STARTED` → 🔴 DEFERRED

- ❌ 7 天真实项目 soak 未启动。

**降级决策**：明确 DEFERRED 至 v1.1.1 RC 前置门禁；不阻断 v1.1.0-rc.1 标签，但要求 v1.1.1 标签必须 7 天 soak 通过。

---

## 第 8 条 · README、CHANGELOG、用户文档与真实能力一致

**状态**：`DONE` → 🟢

- ✅ `CHANGELOG.md` 加入 `1.1.0-rc.1` 段，按 Conventional Commits 聚合；
- ✅ `docs/RELEASE_NOTES_V1.1.0.md` 用户视角新增；
- ✅ `docs/v1.1.0-rc-checklist.md`（本文件）新增；
- 🟡 `README.md` 顶部版本号同步将在 `npm run version:sync` 后由 release worker 提交。

**降级决策**：当前 RC 签字无影响。

---

## 第 9 条 · 所有实验性能力有清晰标签和降级路径

**状态**：`DONE` → 🟢

- ✅ `110-A7` MCP App Canvas / `110-A15` Protocol Gateway / `110-A19` Semantic Code Intelligence / `110-A23` Reproducible Environment Capsule 均为 `Anchored` 状态，不进入 v1.1.0 必交付；
- ✅ `110-X1` A2A Delegation / `110-A14` Agent Benchmark Lab 保持 `Candidate`，明确 Lab 性质；
- ✅ Fleet View（`src/routes/fleet/+page.svelte`）在 v1.2.0 路线图（`docs/PLAN_FLEET_VIEW_V1.2.0.md`）。

**降级决策**：当前 RC 签字无影响。

---

## 第 10 条 · 不携带已知 mock 能力伪装成正式功能

**状态**：`DONE` → 🟢

- ✅ `executeAgent` mock 在 `src/routes/multi-agent/+page.svelte` 标签明确为「Multi-Agent Lab」，不暴露为 GA；
- ✅ Teams chat 范围限制在 `docs/PLAN_V1.1.0.md` §2.2 `Deferred`；
- ✅ Visual / MCP / Browser / Dev Container 等实验性能力不进入主菜单；
- ✅ Attention Queue 与 Task Core 全部对接真实 WAL，不存在 mock 路径。

**降级决策**：当前 RC 签字无影响。

---

## 第 11 条 · macOS 主窗口圆角与原生窗口几何一致 + 圆角缺口 sidebar surface 连续 + 通过截图回归

**状态**：`PARTIAL` → 🟡 跟踪

- ✅ 主内容圆角与 macOS 窗口外轮廓在浅色 / 深色主题、native glass on/off、sidebar 折叠 / 展开下视觉连续（`2b5ec6cb`）；
- ✅ `.app-main-shell` 与 `sidebar-main-corner-bridge` 与原生 glass wash 对齐；
- 🟡 截图视觉回归脚本尚未自动化入库；当前以手工肉眼 + 单元测试覆盖。

**降级决策**：以 PARTIAL 状态进入 RC；自动化视觉回归脚本列为 v1.1.1 跟踪项。

---

## 第 12 条 · 关键 Runtime / MCP / Skill 具备来源、版本、能力和权限证明

**状态**：`PARTIAL` → 🟡 跟踪

- ✅ Runtime 来源 / 版本 / 能力在 `RuntimeCapabilities` typed mirror + `runtime_hub_*` 命令集合；
- ✅ MCP 工具 Schema 在 `src-tauri/src/mcp/*`；
- 🟡 Skill 来源 / 版本 / 权限在 `src-tauri/src/skill_sources/*`，但发布者签名、来源校验与 Capability Attestation Engine 全链路未齐；
- 🟡 Tool call origin / permission decision 进入 `Local Agent Trace` 的覆盖率为部分。

**降级决策**：以 PARTIAL 状态进入 RC；Capability Attestation 全链路列为 v1.1.1 跟踪项。

---

## 最终签字建议

| 维度 | 评估 |
| ---- | ---- |
| 阻断级 P0 风险 | 0 |
| 高风险 P0（需 DEFERRED） | 11 项，全部有书面降级决策 |
| 完成 P0 | 2 项（`110-A21`、`110-S1`） |
| 实验性能力 | 全部 `Anchored` / `Candidate`，未冒充正式功能 |
| 真实能力与文档一致性 | ✅ |
| Mock 伪装 | 无 |
| RC 流水线 | `npm run verify:rc` 待跑（详见 `docs/RELEASE_PROCESS.md`） |

**建议**：
- ✅ 可签 `v1.1.0-rc.1` 标签（前提是 `npm run verify:rc` 通过）；
- 🔴 `v1.1.0` GA 标签需在 7 天 soak + Tier 1 全量 E2E + 关键 p95 采样 三项门禁通过后才签；
- 🟢 所有跟踪项列入 v1.1.1 RC 的硬性前置门禁清单。
