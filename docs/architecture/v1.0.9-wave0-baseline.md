# v1.0.9 Wave 0 Baseline Report

> **Date**: 2026-06-22
> **Base commit**: ca41bf45
> **Branch**: integration/v1.0.9-runtime-hub
> **Worktree**: /Users/yanghaoze/Desktop/PROJECT/miwarp-v109-integration

## 1. Confirmed invariants

- `arch:check` (direction + layers + cycle + file-budget + tauri-contract +
  ios-ws-contract): 0 violations
- `cargo clippy --lib --tests -- -D warnings`: 0 warnings
- `svelte-check`: 0 errors / 0 warnings (1457 files)
- `prettier --check`: clean
- `vitest run`: 1689 / 1689 passing in 3.18s
- `cargo test --lib`: 467 / 467 passing in 3.03s
- i18n:check: 0 errors, 5 pre-existing untranslated-key warnings (carry-over
  from v1.0.8, not in scope for v1.0.9 to fix unless touched)

## 2. Existing pre-merge state (must be respected, not re-implemented)

| Branch | Worktree | Status |
|---|---|---|
| `feat/v1.0.9-runtime-trust-permissions` (ca41bf45) | `~/.devspace/worktrees/miwarp-2e59974b` | HEAD; has uncommitted user edits in `docs/PLAN_V1.0.9.md`, `permission-coordinator.ts`, `permission-coordinator/queries.ts` |
| `feat/v1.0.9-trustworthy-desktop` (7ac56f22) | `~/.devspace/worktrees/miwarp-ebdfdb23` | Phase 1 only; has uncommitted edit in `send-coordinator.ts` |
| `refactor/ios-cohesion-quality` (99ecc50a) | `~/.devspace/worktrees/miwarp-73ae7a33` | untracked `.build/` directory |
| `feat/core-reliability-foundation` (8e3ec898) | `~/.devspace/worktrees/miwarp-7c3e07cc` | untracked `.gradle/`, `.build/`, `NEXT_SESSION_CONTEXT.md` |
| `refactor/frontend-cohesion-quality` (dc840cff) | `~/.devspace/worktrees/miwarp-a379acd6` | clean |

The uncommitted content in those worktrees is **user's in-flight work**;
it MUST NOT be touched, stashed, reset, or cleaned. The integration
worktree starts from the committed `ca41bf45` only.

The main `/Users/yanghaoze/Desktop/PROJECT/miwarp` has untracked items
(`.codex/config.toml`, `apps/ios/MiWarpMobile/build-ipad/`,
`apps/ios/MiWarpMobile/build-test/`) that are private user files —
forbidden to delete, move, or commit.

## 3. Codebase size snapshot (today's reality, not the target)

| File | Lines | Wave 1/2/3 target |
|---|---|---|
| `src/lib/stores/session-store.svelte.ts` | 3441 | shrink via E (workbench pull-out) + A (run state reduction) |
| `src-tauri/src/agent/claude_protocol.rs` | 3409 | B: strangler to per-event-family adapters |
| `src/routes/+layout.svelte` | 3139 | E: split workbench shell off |
| `src-tauri/src/agent/session_actor.rs` | 2824 | A: extract recovery / respawn / typed errors |
| `src-tauri/src/commands/session.rs` | 2806 | A: split into per-domain files |
| `src/lib/components/PromptInput.svelte` | 2376 | E: extract workbench + send UI; A: thin send |
| `src/routes/chat/+page.svelte` | 1844 | A+C+E: extract workbench and trust center |
| `src/lib/api.ts` | 1643 | A/B/D split by domain |
| `src/lib/components/InlineToolCard.svelte` | 1207 | C: per-card projection; split |

Targets above are not committed here — they are the integrator's
compass. They will be revised by each agent's actual diff.

## 4. Contracts frozen in this wave

Three documents added to `docs/architecture/`:

- `v1.0.9-runtime-contract.md` — Runtime Hub + Provider + Model
  separation, capability flags, transport boundary, forbidden patterns.
- `v1.0.9-transaction-contracts.md` — SendTransaction,
  PermissionTransaction, RecoveryState, ActorLifecycle, plus the
  inherited Phase 1/2 invariants that must not be duplicated.
- `v1.0.9-shared-file-ownership.md` — per-agent file ownership table,
  coordination rules, and forbidden modifications.

## 5. Performance baseline (approximate; F will refine)

| Metric | Value (Wave 0) |
|---|---|
| `npm run build` (Vite) | ~14.6 s (cold) |
| `cargo clippy --lib --tests` | ~10 s (incremental) |
| `vitest run` | 3.18 s for 1689 tests |
| `cargo test --lib` | 3.03 s for 467 tests |
| `svelte-check` | runs to completion in seconds |
| `arch:check` (all six) | runs to completion in seconds |

Cold-start Tauri and 10k-event Timeline render time are not measured
in this wave — Agent F owns that benchmark and will report a
delta-vs-baseline number.

## 6. Wave 1 readiness checklist

- [x] Integration worktree created from ca41bf45
- [x] Dependencies installed (`npm install`)
- [x] Frontend built (so `cargo` macro can include `$CARGO_MANIFEST_DIR/../build`)
- [x] Baseline checks all green
- [x] Contract documents committed
- [ ] Wave 1 agent worktrees created
- [ ] Wave 1 agents dispatched
- [ ] Wave 1 contract tests passing (Agent H)

## 7. Open questions for user

1. Is the `integration/v1.0.9-runtime-hub` branch name acceptable, or do
   you want a different one?
2. The uncommitted edits in the existing v1.0.9 worktrees
   (`permission-coordinator/queries.ts`, `permission-coordinator.ts`,
   `docs/PLAN_V1.0.9.md`, `send-coordinator.ts`) — are these expected
   to be merged into the integration branch before Wave 1 starts, or
   should they be ignored for now? My current plan: ignore them; only
   commit content from new Wave 1+ agents.
3. Version sync from `1.0.8` → `1.0.9` should happen at Wave 4
   integration, not now. Confirm?
4. `npm run verify` is the Wave 5 gate. Wave 3 per-branch gate uses
   the lighter `arch:check + check + lint + format:check + i18n:check +
   test + build + rust:check + cargo test --lib`. Confirm?

## 8. Commit

This baseline + the three contract documents will be committed on
`integration/v1.0.9-runtime-hub` as the first commit of the branch.
No push will happen.
